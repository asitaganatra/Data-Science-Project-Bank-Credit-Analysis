# Streamlit secrets template for Streamlit Cloud deployment
# Copy this to .streamlit/secrets.toml and fill in your values if needed
# Note: For this project, all data is embedded; no external API keys required.

# Example if you need to add external data source later:
# database_url = "your_database_url_here"
# api_key = "your_api_key_here"
